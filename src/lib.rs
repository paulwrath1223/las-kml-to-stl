mod height_map;
mod las_resampler;
mod errors;
mod utils;
mod utm_bounds;
mod utm_coord;
mod mask;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
